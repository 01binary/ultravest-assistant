{
  "name": "ultravest-assistant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src",
    "test": "mocha --recursive --require ./test/setup.js --compilers js:babel-register",
    "test:watch": "yarn test -- --watch",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "lint && preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "babel": {
    "presets": [
      "env"
    ],
    "plugins": [
      [
        "transform-react-jsx",
        {
          "pragma": "h"
        }
      ]
    ]
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "eslint": "^4.6.1",
    "eslint-config-synacor": "^1.1.1",
    "if-env": "^1.0.0",
    "mocha": "^3.5.0",
    "preact-cli": "^1.4.1",
    "sass-lint": "^1.11.1",
    "undom": "^0.3.3"
  },
  "dependencies": {
    "node-sass": "^4.5.3",
    "preact": "^8.2.5",
    "preact-compat": "^3.17.0",
    "preact-jsx-chai": "^2.2.1",
    "react": "npm:preact-compat",
    "recompose": "^0.25.0",
    "sass-loader": "^6.0.6"
  }
}
