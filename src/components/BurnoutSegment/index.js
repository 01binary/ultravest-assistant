import { h } from 'preact';
import style from './style';

/**
 * Burnout diagram secment.
 * @param {*} props 
 */
const BurnoutSegment = ({  }) => (
	<article>

	</article>
);

export default BurnoutSegment;